diff --git a/dist/module/history/getHistory.js b/dist/module/history/getHistory.js
index 819261e54030086b47c7c28010ef056f9aa0ae30..d8c8fb1fb9f4ac139006c0398387128fffffbafb 100644
--- a/dist/module/history/getHistory.js
+++ b/dist/module/history/getHistory.js
@@ -160,6 +160,8 @@ export const getHistory = async ({ addresses, provider, unit = "sat", fromHeight
                             : undefined,
                     }
                     : undefined,
+                outpointTransactionHash: getOutpointHash(input.outpointTransactionHash),
+                outpointIndex: input.outpointIndex,
             };
         });
         result.outputs = tx.outputs.map((output) => {
diff --git a/dist/module/history/interface.d.ts b/dist/module/history/interface.d.ts
index 2eebf72de8847028c08edceacfc0d9b4ad4618b6..797408ceb91fdefac4eb581d39c2e3149cc81a82 100644
--- a/dist/module/history/interface.d.ts
+++ b/dist/module/history/interface.d.ts
@@ -3,6 +3,8 @@ export interface InOutput {
     address: string;
     value: number;
     token?: TokenI;
+    outpointTransactionHash?: string;
+    outpointIndex?: number;
 }
 export interface TransactionHistoryItem {
     inputs: InOutput[];
diff --git a/dist/module/wallet/Base.js b/dist/module/wallet/Base.js
index 2341e3372ef8fda0c23aca420b8cb53c8b306fc4..58d9eff078520ad25c1c7a75710cd66944fb27fa 100644
--- a/dist/module/wallet/Base.js
+++ b/dist/module/wallet/Base.js
@@ -905,7 +905,10 @@ export class BaseWallet {
             bucket.push(request);
             requestsByCategory.set(request.category, bucket);
         }
-        const utxos = await this.getUtxos();
+        let utxos = await this.getUtxos();
+        if (options?.utxoIds) {
+            utxos = checkUtxos(options.utxoIds.map((utxoId) => typeof utxoId === "string" ? fromUtxoId(utxoId) : utxoId), utxos);
+        }
         const ensureUtxos = [];
         const outputs = [];
         for (const [category, requests] of requestsByCategory) {
@@ -970,7 +973,10 @@ export class BaseWallet {
         if (burnRequests.length === 0) {
             throw new Error("tokenBurn requires at least one request");
         }
-        const utxos = await this.getUtxos();
+        let utxos = await this.getUtxos();
+        if (options?.utxoIds) {
+            utxos = checkUtxos(options.utxoIds.map((utxoId) => typeof utxoId === "string" ? fromUtxoId(utxoId) : utxoId), utxos);
+        }
         const ensureUtxos = [];
         const changeSendRequests = [];
         for (const burnRequest of burnRequests) {
